services:
  - type: web
    name: nitex-app
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npx serve -s dist/app/browser
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: PORT
        value: 10000
